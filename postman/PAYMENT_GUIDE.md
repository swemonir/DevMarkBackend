# 💰 Payment System Guide

## 📥 Import Collection
`postman/Payment_API.postman_collection.json` টি Postman এ Import করুন।

## 🔄 Workflow (কিভাবে টেস্ট করবেন)

**Prerequisites:**
1. একটি **Marketplace Listing** থাকতে হবে (`listing_id` environment variable এ set থাকা লাগবে)।
   - Marketplace API collection থেকে "List in Marketplace" run করে `listing_id` পেতে পারেন।
2. **Buyer Token** থাকতে হবে (`buyer_token`)।
   - Seller এবং Buyer ভিন্ন ইউজার হতে হবে!

---

### Step 1: Initiate Payment
`POST /api/payments/initiate`
- এটি পেমেন্ট শুরু করবে এবং একটি Pending record তৈরি করবে।
- **Returns:** `paymentId`, `transactionId` (Mock)

### Step 2: Verify Payment
`POST /api/payments/verify`
- এটি পেমেন্ট কনফার্ম করবে।
- **Action:** 
  1. Payment status `completed` হবে।
  2. Project টি `sold` হিসেবে মার্ক হবে (`soldTo` buyer ID দিয়ে সেট হবে)।
  3. Marketplace থেকে প্রজেক্টটি সরে যাবে (যেহেতু `isForSale: false` হয়ে যাবে)।

### Step 3: Check History
`GET /api/transactions`
- আপনার সব ক্রয়ের লিস্ট দেখতে পাবেন।

---

## 🛠️ Code Structure Explainer for Beginners

আমরা ৩টি লেয়ার ব্যবহার করেছি যাতে কোড ক্লিন থাকে:

1. **Model (`Payment.model.js`)**: ডেটাবেস ডিজাইন। আমরা `user`, `project`, `amount`, এবং `status` রেখেছি। `transactionId` ব্যবহার করেছি যাতে ভবিষ্যতে Stripe/SSLCommerz ইন্টিগ্রেট করা সহজ হয়।

2. **Controller (`payment.controller.js`)**: 
   - `initiatePayment`: প্রথমে চেক করি প্রজেক্টটি বিক্রির জন্য আছে কিনা। তারপর Pending পেমেন্ট তৈরি করি।
   - `verifyPayment`: এখানে আমরা **MongoDB Transaction (`session`)** ব্যবহার করেছি। কেন? 
     - যাতে টাকা কাটার পর প্রজেক্টের স্ট্যাটাস আপডেট না হলে পুরো প্রসেস রোলব্যাক হয়। এটি ডেটা কনসিস্টেন্সি নিশ্চিত করে।

3. **Routes (`paymentRoutes.js`)**: URL গুলো সাজানো হয়েছে।

Happy Coding! 🚀
