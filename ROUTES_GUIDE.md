# 📋 Complete API Routes Guide - DevMark Backend

## 🗂️ পূর্ণ রাউট স্ট্রাকচার

### **🔐 Authentication Routes** (ইতিমধ্যে তৈরি ✅)
```
POST /api/auth/register     - নতুন ইউজার রেজিস্টার
POST /api/auth/login        - ইউজার লগইন
GET  /api/auth/me          - বর্তমান ইউজার তথ্য
POST /api/auth/refresh     - টোকেন রিফ্রেশ
POST /api/auth/logout      - লগআউট
```

### **👤 User Management Routes**
```
GET    /api/users           - সব ইউজার লিস্ট (Admin)
GET    /api/users/:id       - নির্দিষ্ট ইউজার তথ্য
PUT    /api/users/:id       - ইউজার আপডেট
DELETE /api/users/:id       - ইউজার ডিলেট
PUT    /api/users/profile   - নিজের প্রোফাইল আপডেট
POST   /api/users/avatar    - প্রোফাইল ছবি আপলোড
```

### **🚀 Project Management Routes**
```
GET    /api/projects              - সব প্রজেক্ট লিস্ট
POST   /api/projects              - নতুন প্রজেক্ট তৈরি
GET    /api/projects/:id          - নির্দিষ্ট প্রজেক্ট তথ্য
PUT    /api/projects/:id          - প্রজেক্ট আপডেট
DELETE /api/projects/:id          - প্রজেক্ট ডিলেট
POST   /api/projects/:id/submit   - রিভিউয়ের জন্য সাবমিট
PUT    /api/projects/:id/approve  - প্রজেক্ট অ্যাপ্রুভ (Admin)
PUT    /api/projects/:id/reject   - প্রজেক্ট রিজেক্ট (Admin)
POST   /api/projects/:id/media    - মিডিয়া আপলোড
```

### **🏪 Marketplace Routes**
```
GET    /api/marketplace           - মার্কেটপ্লেস লিস্টিং
POST   /api/marketplace           - নতুন লিস্টিং তৈরি
GET    /api/marketplace/:id       - নির্দিষ্ট লিস্টিং তথ্য
PUT    /api/marketplace/:id       - লিস্টিং আপডেট
DELETE /api/marketplace/:id       - লিস্টিং ডিলেট
POST   /api/marketplace/:id/buy   - প্রজেক্ট ক্রয়
GET    /api/marketplace/search    - সার্চ ও ফিল্টার
```

### **💰 Payment & Transaction Routes**
```
POST   /api/payments/initiate     - পেমেন্ট শুরু
GET    /api/payments/:id          - পেমেন্ট স্ট্যাটাস
POST   /api/payments/verify       - পেমেন্ট ভেরিফাই
GET    /api/transactions          - ট্রানজেকশন হিস্টরি
```

### **📊 Analytics Routes**
```
GET    /api/analytics/dashboard   - ড্যাশবোর্ড ডাটা
GET    /api/analytics/projects    - প্রজেক্ট অ্যানালিটিক্স
GET    /api/analytics/users       - ইউজার অ্যানালিটিক্স
GET    /api/analytics/sales       - সেলস অ্যানালিটিক্স
```

### **🔔 Notification Routes**
```
GET    /api/notifications         - নোটিফিকেশন লিস্ট
PUT    /api/notifications/:id/read - নোটিফিকেশন রিড
POST   /api/notifications/send    - নোটিফিকেশন পাঠানো
```

### **⭐ Review & Rating Routes**
```
POST   /api/reviews               - রিভিউ দেওয়া
GET    /api/reviews/:projectId    - প্রজেক্ট রিভিউ
PUT    /api/reviews/:id           - রিভিউ আপডেট
DELETE /api/reviews/:id           - রিভিউ ডিলেট
```

### **🔍 Search & Filter Routes**
```
GET    /api/search/projects       - প্রজেক্ট সার্চ
GET    /api/search/users          - ইউজার সার্চ
GET    /api/search/marketplace    - মার্কেটপ্লেস সার্চ
GET    /api/filters/categories    - ক্যাটাগরি লিস্ট
GET    /api/filters/tags          - ট্যাগ লিস্ট
```
---

## 🎯 কোন রাউট দিয়ে কি কাজ হবে

### **👤 ইউজার ম্যানেজমেন্ট**
- **রেজিস্ট্রেশন/লগইন**: `POST /api/auth/register`, `POST /api/auth/login`
- **প্রোফাইল ম্যানেজমেন্ট**: `PUT /api/users/profile`, `POST /api/users/avatar`
- **ইউজার তালিকা**: `GET /api/users` (Admin এর জন্য)

### **🚀 প্রজেক্ট ম্যানেজমেন্ট**
- **প্রজেক্ট CRUD**: `GET/POST/PUT/DELETE /api/projects`
- **প্রজেক্ট সাবমিশন**: `POST /api/projects/:id/submit`
- **অ্যাপ্রুভাল**: `PUT /api/projects/:id/approve` (Admin)
- **মিডিয়া আপলোড**: `POST /api/projects/:id/media`

### **🏪 মার্কেটপ্লেস**
- **লিস্টিং CRUD**: `GET/POST/PUT/DELETE /api/marketplace`
- **কেনাকাটা**: `POST /api/marketplace/:id/buy`
- **সার্চ**: `GET /api/marketplace/search`

### **💰 পেমেন্ট সিস্টেম**
- **পেমেন্ট প্রসেসিং**: `POST /api/payments/initiate`
- **ট্রানজেকশন হিস্টরি**: `GET /api/transactions`

### **📊 অ্যানালিটিক্স ও রিপোর্টিং**
- **ড্যাশবোর্ড ডাটা**: `GET /api/analytics/dashboard`
- **প্রজেক্ট স্ট্যাটিস্টিক্স**: `GET /api/analytics/projects`

---

## 🏗️ ইমপ্লিমেন্টেশন অর্ডার

### **Phase 1: বেসিক (এখন করা)**
1. ✅ Authentication Routes
2. 🔄 User Profile Management
3. 📁 Project CRUD Operations

### **Phase 2: অ্যাডভান্সড**
4. 🏪 Marketplace System
5. 💰 Payment Integration
6. 📊 Analytics Dashboard

### **Phase 3: প্রোফেশনাল**
7. 🔔 Notification System
8. ⭐ Review & Rating
9. 🔍 Advanced Search

---

## 🛠️ কোন ফাইলে কি থাকবে

```
src/app/routes/
├── auth.js              ✅ (তৈরি)
├── users.js             👤 (পরবর্তী)
├── projects.js          🚀 (পরবর্তী)
├── marketplace.js       🏪 (পরবর্তী)
├── payments.js          💰 (পরবর্তী)
├── analytics.js         📊 (পরবর্তী)
└── notifications.js     🔔 (পরবর্তী)
```

---

## 🎯 পরবর্তী স্টেপ

**আপনি কি করতে চান?**

1. **👤 User Management Routes** তৈরি করতে?
2. **🚀 Project Management Routes** তৈরি করতে?
3. **🏪 Marketplace Routes** তৈরি করতে?
4. **কোনো নির্দিষ্ট রাউট** step-by-step তৈরি করতে?

বলুন আপনি কোন রাউট থেকে শুরু করতে চান! 🚀
